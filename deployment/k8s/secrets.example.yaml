# Template only — do not commit real values. Generate with:
#   kubectl create secret generic rankiq-secrets \
#     --from-literal=api-auth-token=<real-token> \
#     --from-literal=pgvector-dsn=<real-dsn-if-using-pgvector>

apiVersion: v1
kind: Secret
metadata:
  name: rankiq-secrets
type: Opaque
stringData:
  api-auth-token: "REPLACE_ME"
  pgvector-dsn: "postgresql://rankiq:REPLACE_ME@postgres-host:5432/rankiq"
